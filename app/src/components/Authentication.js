import React from 'react';

import Parse from 'parse';


class Authentication extends React.Component {

    constructor(props) {
            super(props);

            this.state = {
            };
        }



    render() {

        return (
          <div>
            <a className="logo">G surf</a>
          </div>

        );
    }
}

Authentication.propTypes = {
};

Authentication.defaultProps = {
}

export default Authentication;
